<?php
    // create  tags coming from a $options variable
    // This is to be used by AJAX in order to fill the contents of a combo
    // box
			echo $this->Form->input('Vente.plat_id',array('options'=>$plats));
 
?>

